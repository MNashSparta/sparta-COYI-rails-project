<link rel="stylesheet" href="/Stylesheets/news.css">
<p id="notice"><%= notice %></p>
<div class="row news-title">
<div class="col-md-10">
  <h1>News</h1>
</div>
<div class="col-md-2">
  <button class="button-style" type="button" name="button" value="<%= link_to 'New Article', new_news_path %>"></button> 
</div>
</div>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Story</th>
      <th>Published</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <!-- <tbody> -->
    <!-- <% @news.each do |news| %>
      <tr>
        <td><%= news.title %></td>
        <td><%= news.story %></td>
        <td><%= news.created_at %></td>
        <td><%= news.user %></td>
        <td><%= link_to 'Show', news %></td>
        <td><%= link_to 'Edit', edit_news_path(news) %></td>
        <td><%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %> -->
    <%= render 'news/news_list' %>
  <!-- </tbody> -->
</table>

<br>
